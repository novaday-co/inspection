name: inspection
description: validation forms and data in Dart and Flutter like Laravel framework
version: 0.0.4
homepage: https://github.com/faridfr/inspection

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: